# national-flag-quiz

UI 練習用の国旗クイズです。`React + Vite + TypeScript` で構成し、縦スワイプで国旗カードを送るフラッシュカード風のミニアプリにしています。

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

## Notes

- 国旗は絵文字で表示
- 一定時間後に国名、さらに少し後に首都と地域を表示
- `vite.config.ts` で GitHub Pages 向けの `base` を設定
